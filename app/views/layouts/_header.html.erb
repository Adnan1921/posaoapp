<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">

<style>
	.navbar-dark .navbar-nav .nav-link  {
	    color: #fff !important;
			font-size: 18px;
	}
</style>
<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <div class="container-fluid">
		
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
      <li class="nav-item" >
        <%= link_to 'Poslovi', posaos_path, :class =>"nav-link"%>
      </li>
      <li class="nav-item">
				<% if user_signed_in? %>
       <%= link_to 'Dodaj posao', new_posao_path, :class =>"nav-link" %>
				
				<% end %>
      </li>

        
      </ul>
			 <% if user_signed_in? %>
			   <strong><%= current_user.email %></strong>.
	       <li class="nav-item">
				 
			   <%= link_to 'Uredi profil', edit_user_registration_path %> |
			   <%= link_to "Logout", destroy_user_session_path, method: :delete  %>
			 <% else %>
			   <%= link_to "Kreiraj svoj racun", new_user_registration_path  %> |
			   <%= link_to "Loguj se", new_user_session_path   %>
			 <% end %>
		 </li>
    </div>
  </div>
</nav>
    

    <!-- Bootstrap core CSS -->

    <!-- <div>
			<nav class="navbar navbar-expand-lg navbar navbar-dark bg-primary">
			  <a class="navbar-brand" >MOJPOSAO.HR</a>
			  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
			    <span class="navbar-toggler-icon"></span>
			  </button>
			  <div class="collapse navbar-collapse" id="navbarNav">
			    <ul class="navbar-nav">

			      <li class="nav-item">
			        <%= link_to 'Poslovi', posaos_path, :class =>"nav-link"%>
			      </li>
			      <li class="nav-item">
							<% if user_signed_in? %>
			       <%= link_to 'Dodaj posao', new_posao_path, :class =>"nav-link" %>

							<% end %>
			      </li>
			      <li class="nav-item">

<p class="navbar-text float-right">
<% if user_signed_in? %>
  <strong><%= current_user.email %></strong>.
  <%= link_to 'Uredi profil', edit_user_registration_path, :class => 'navbar-link' %> |
  <%= link_to "Logout", destroy_user_session_path, method: :delete, :class => 'navbar-link'  %>
<% else %>
  <%= link_to "Kreiraj svoj racun", new_user_registration_path, :class => 'navbar-link'  %> |
  <%= link_to "Loguj se", new_user_session_path, :class => 'navbar-link'  %>
<% end %>
</p>			      </li>
			    </ul>
			  </div>
			</nav>
</div> -->